services:
  spark-master:
    image: spark:4.1.1-scala2.13-java21-ubuntu
    entrypoint: ['/opt/spark/sbin/start-master.sh']
    environment:
      - SPARK_NO_DAEMONIZE=true
      - SPARK_MODE=master
      - SPARK_RPC_AUTHENTICATION_ENABLED=no
      - SPARK_RPC_ENCRYPTION_ENABLED=no
      - SPARK_LOCAL_STORAGE_ENCRYPTION_ENABLED=no
      - SPARK_SSL_ENABLED=no
      - SPARK_USER=spark
      - SPARK_EVENTLOG_ENABLED=true
      - SPARK_EVENTLOG_DIR=file:/mnt/spark-events
    hostname: spark-master
    ports:
      - '8080:8080'
      - '7077:7077'
    volumes:
      - spark-events:/mnt/spark-events/
      - spark-job-data:/mnt/data/

  spark-history-server:
    image: spark:4.1.1-scala2.13-java21-ubuntu
    entrypoint: ['/opt/spark/sbin/start-history-server.sh']
    environment:
      - SPARK_NO_DAEMONIZE=true
      - SPARK_HISTORY_OPTS=-Dspark.history.fs.logDirectory=file:/mnt/spark-events -Dspark.history.ui.port=18080
      - SPARK_USER=spark
    hostname: spark-history-server
    ports:
      - '18080:18080'
    volumes:
      - spark-events:/mnt/spark-events/

  spark-worker:
    image: spark:4.1.1-scala2.13-java21-ubuntu
    deploy:
      replicas: 5
    entrypoint: ['/opt/spark/sbin/start-worker.sh', 'spark://spark-master:7077']
    environment:
      - SPARK_NO_DAEMONIZE=true
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark-master:7077
      - SPARK_WORKER_MEMORY=8G
      - SPARK_WORKER_CORES=8
      - SPARK_RPC_AUTHENTICATION_ENABLED=no
      - SPARK_RPC_ENCRYPTION_ENABLED=no
      - SPARK_LOCAL_STORAGE_ENCRYPTION_ENABLED=no
      - SPARK_SSL_ENABLED=no
      - SPARK_USER=spark
      - SPARK_EVENTLOG_ENABLED=true
      - SPARK_EVENTLOG_DIR=file:/mnt/spark-events
    ports:
      - '8081'
    volumes:
      - spark-events:/mnt/spark-events/
      - spark-job-data:/mnt/data/

  spark-driver:
    image: me.kosik/data-generator-intervals:1.0.4
    environment:
      - SPARK_NO_DAEMONIZE=true
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark-master:7077
      - SPARK_RPC_AUTHENTICATION_ENABLED=no
      - SPARK_RPC_ENCRYPTION_ENABLED=no
      - SPARK_LOCAL_STORAGE_ENCRYPTION_ENABLED=no
      - SPARK_SSL_ENABLED=no
      - SPARK_USER=spark
      - SPARK_EVENTLOG_ENABLED=true
      - SPARK_EVENTLOG_DIR=file:/mnt/spark-events
    hostname: spark-driver
    ports:
      - '4040:4040'
    volumes:
      - spark-events:/mnt/spark-events/
      - spark-job-data:/mnt/data/
    deploy:
      resources:
        limits:
          memory: 4g

volumes:

  spark-events:
    external: true
    name: spark-events

  spark-job-data:
    external: true
    name: spark-job-data
